[tool.poetry]
name = "discord-gemini-bot"
version = "0.1.0"
description = "A Discord bot using Gemini AI"
authors = ["ScareTix"]

[tool.poetry.dependencies]
python = "^3.8"
"discord.py" = "*"
flask = "*"
requests = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
